Write a function that takes a string in the alphabet "abcd" and performs the
actions:

* Delete every "b"
* Replace every "a" with "dd"

You may assume the input array has enough space for the result.
