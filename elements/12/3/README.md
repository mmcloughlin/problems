Search a sorted array of *distinct integers* for an entry equal to its index.


Consider the predicate

  A[i] >= i

If this is true once, it must always be true, since A[i] must increase by at
least one.
